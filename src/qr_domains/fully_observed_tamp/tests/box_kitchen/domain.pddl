(define
  (domain box-kitchen)

  (:object-types
  )

  (:predicates
    (on ?obj ?region)
    (available-hand ?hand)
    (holding ?obj)
    (graspable ?obj)
    (support-surface ?obj)
    (robot ?robot)
    (workspace ?bounds)
    (use-right)
    (use-left)
    (use-base)
  )
)
